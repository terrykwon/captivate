import setuptools

setuptools.setup(
    name="visual-attention",
    version="0.0.1",
    author="misc",
    description="detecting attended visual targets in video",
    python_requires='>=3.6',
    packages=setuptools.find_packages(),
)